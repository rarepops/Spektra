<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:Spektra.App"
        x:Class="Spektra.App.FolderManifestWindow"
        Title="Folder Manifest" Width="760" Height="640"
        Background="#111111" Foreground="#CCCCCC" FontSize="13"
        DragDrop.AllowDrop="True">
  <DockPanel Margin="12">
    <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" Margin="0,0,0,8">
      <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Folder}" Foreground="#EEEEEE"
                 VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                 ToolTip.Tip="{Binding Folder}" />
      <TextBox x:Name="ManifestPathBox" Grid.Row="1" Grid.Column="0" Margin="0,6,0,0"
               MinHeight="26" Padding="6,3" VerticalContentAlignment="Center"
               PlaceholderText="Paste or type a folder path and press Enter"
               KeyDown="OnManifestPathKeyDown"
               ToolTip.Tip="Lists the folder; dragging a folder from Explorer works too" />
      <TextBox Grid.Row="2" Grid.Column="0" Margin="0,6,0,0"
               MinHeight="26" Padding="6,3" VerticalContentAlignment="Center"
               Text="{Binding FilterText}"
               PlaceholderText="Filter by extension, space separated: flac jpg nfo"
               ToolTip.Tip="Shows only files whose kind or extension matches; folders without matches disappear and Export writes exactly what is shown" />
      <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" Margin="8,0,0,0" Spacing="6" Width="130">
        <Button Content="Browse…" Click="OnPickFolderClicked" HorizontalAlignment="Stretch" />
        <Button Content="Export…" Click="OnExportClicked" HorizontalAlignment="Stretch"
                ToolTip.Tip="Save the tree as HTML, CSV, or JSON" />
      </StackPanel>
    </Grid>

    <!-- Full-width divider between the input bar and the listing; the negative
         horizontal margin cancels the DockPanel's 12px inset so the line reaches
         both window edges like the main view. The column headers' lane widths
         (64/68 plus the row grid's 4px right margin) mirror the file rows. -->
    <Border DockPanel.Dock="Top" Height="1" Background="#2A2A2A" Margin="-12,0,-12,6" />
    <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto,Auto" Margin="0,0,6,6">
      <TextBlock Grid.Column="0" Text="Name" Foreground="#777777" FontSize="11" />
      <TextBlock Grid.Column="1" Text="Kind" Foreground="#777777" FontSize="11" Width="64" />
      <TextBlock Grid.Column="2" Text="Size" Foreground="#777777" FontSize="11" Width="68"
                 TextAlignment="Right" />
    </Grid>

    <TextBlock DockPanel.Dock="Bottom" Margin="0,6,0,0" Text="{Binding FooterText}"
               Foreground="#888888" FontSize="11" />
    <Border DockPanel.Dock="Bottom" Height="1" Background="#2A2A2A" Margin="-12,8,-12,0" />

    <TreeView ItemsSource="{Binding RootItems}" Background="Transparent">
      <TreeView.Styles>
        <Style Selector="TreeViewItem">
          <Setter Property="MinHeight" Value="22" />
          <Setter Property="Padding" Value="2,1" />
        </Style>
        <Style Selector="TreeView > TreeViewItem">
          <Setter Property="IsExpanded" Value="{Binding IsExpanded}" />
        </Style>
      </TreeView.Styles>
      <TreeView.DataTemplates>
        <TreeDataTemplate DataType="app:ManifestFolderItem" ItemsSource="{Binding Children}">
          <TextBlock Text="{Binding Header}" Foreground="#EEEEEE" />
        </TreeDataTemplate>
        <DataTemplate DataType="app:ManifestFileItem">
          <!-- Fixed lane widths keep every row's kind and size under the
               column headers above the tree. -->
          <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,0,4,0" ToolTip.Tip="{Binding Path}">
            <TextBlock Grid.Column="0" Text="{Binding Name}" Margin="0,0,8,0"
                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="{Binding Kind}" Foreground="{Binding KindBrush}"
                       FontSize="12" Width="64" TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center" />
            <TextBlock Grid.Column="2" Text="{Binding SizeText}" Foreground="#999999" FontSize="12"
                       Width="68" TextAlignment="Right" VerticalAlignment="Center" />
          </Grid>
        </DataTemplate>
      </TreeView.DataTemplates>
    </TreeView>
  </DockPanel>
</Window>
