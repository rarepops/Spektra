<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="MathNet.Numerics" Version="5.0.0" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.11" />
    <!-- Pins the SQLite engine we ship ahead of Microsoft.Data.Sqlite's own
         transitive bundle, which trailed the e_sqlite3 advisory GHSA-2m69-gcr7-jv3q
         at 2.1.11. The CLI single-file publish bundles this native library: after a
         bump, run the PUBLISHED spektra-cli with its cache, not just the build. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.5" />
  </ItemGroup>

</Project>
